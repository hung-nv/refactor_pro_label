<?php
    $helper = $this->helper('\Swissup\ProLabels\Helper\CatalogHelpler');
    $product = $block->getProduct();
    $labels = $helper->getLabels($product, 'product'); // initialize labels
    $labelsHtml = $helper->renderContentLabels($labels);
    if (!$labelsHtml) {
        return;
    }
?>
<?= $labelsHtml ?>
